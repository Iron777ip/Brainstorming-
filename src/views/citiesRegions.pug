extends layout.pug

block head
  link(rel="stylesheet", href="/cities.css")

block content
  h2 Regions
  table
    thead
      th №
      th Name

    tbody
      each row, i in rows
        tr
          td.fit #{i+1}
          td.fit
            a(href=`/cities/regions/${row.Region}`) #{row.Region}